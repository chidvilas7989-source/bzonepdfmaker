services:
  - type: web
    name: bzone-pdf-maker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DISABLE_LOCAL_OCR
        value: "1"
      - key: GEMINI_API_KEY
        sync: false
